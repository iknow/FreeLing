# Makefile.in generated by automake 1.11.1 from Makefile.am.
# @configure_input@

# Copyright (C) 1994, 1995, 1996, 1997, 1998, 1999, 2000, 2001, 2002,
# 2003, 2004, 2005, 2006, 2007, 2008, 2009  Free Software Foundation,
# Inc.
# This Makefile.in is free software; the Free Software Foundation
# gives unlimited permission to copy and/or distribute it,
# with or without modifications, as long as this notice is preserved.

# This program is distributed in the hope that it will be useful,
# but WITHOUT ANY WARRANTY, to the extent permitted by law; without
# even the implied warranty of MERCHANTABILITY or FITNESS FOR A
# PARTICULAR PURPOSE.

@SET_MAKE@

VPATH = @srcdir@
pkgdatadir = $(datadir)/@PACKAGE@
pkgincludedir = $(includedir)/@PACKAGE@
pkglibdir = $(libdir)/@PACKAGE@
pkglibexecdir = $(libexecdir)/@PACKAGE@
am__cd = CDPATH="$${ZSH_VERSION+.}$(PATH_SEPARATOR)" && cd
install_sh_DATA = $(install_sh) -c -m 644
install_sh_PROGRAM = $(install_sh) -c
install_sh_SCRIPT = $(install_sh) -c
INSTALL_HEADER = $(INSTALL_DATA)
transform = $(program_transform_name)
NORMAL_INSTALL = :
PRE_INSTALL = :
POST_INSTALL = :
NORMAL_UNINSTALL = :
PRE_UNINSTALL = :
POST_UNINSTALL = :
build_triplet = @build@
host_triplet = @host@
subdir = data
DIST_COMMON = $(srcdir)/Makefile.am $(srcdir)/Makefile.in
ACLOCAL_M4 = $(top_srcdir)/aclocal.m4
am__aclocal_m4_deps = $(top_srcdir)/configure.ac
am__configure_deps = $(am__aclocal_m4_deps) $(CONFIGURE_DEPENDENCIES) \
	$(ACLOCAL_M4)
mkinstalldirs = $(install_sh) -d
CONFIG_CLEAN_FILES =
CONFIG_CLEAN_VPATH_FILES =
SOURCES =
DIST_SOURCES =
am__vpath_adj_setup = srcdirstrip=`echo "$(srcdir)" | sed 's|.|.|g'`;
am__vpath_adj = case $$p in \
    $(srcdir)/*) f=`echo "$$p" | sed "s|^$$srcdirstrip/||"`;; \
    *) f=$$p;; \
  esac;
am__strip_dir = f=`echo $$p | sed -e 's|^.*/||'`;
am__install_max = 40
am__nobase_strip_setup = \
  srcdirstrip=`echo "$(srcdir)" | sed 's/[].[^$$\\*|]/\\\\&/g'`
am__nobase_strip = \
  for p in $$list; do echo "$$p"; done | sed -e "s|$$srcdirstrip/||"
am__nobase_list = $(am__nobase_strip_setup); \
  for p in $$list; do echo "$$p $$p"; done | \
  sed "s| $$srcdirstrip/| |;"' / .*\//!s/ .*/ ./; s,\( .*\)/[^/]*$$,\1,' | \
  $(AWK) 'BEGIN { files["."] = "" } { files[$$2] = files[$$2] " " $$1; \
    if (++n[$$2] == $(am__install_max)) \
      { print $$2, files[$$2]; n[$$2] = 0; files[$$2] = "" } } \
    END { for (dir in files) print dir, files[dir] }'
am__base_list = \
  sed '$$!N;$$!N;$$!N;$$!N;$$!N;$$!N;$$!N;s/\n/ /g' | \
  sed '$$!N;$$!N;$$!N;$$!N;s/\n/ /g'
am__installdirs = "$(DESTDIR)$(asdatadir)" "$(DESTDIR)$(asdepdatadir)" \
	"$(DESTDIR)$(asnecdatadir)" "$(DESTDIR)$(cacorefdatadir)" \
	"$(DESTDIR)$(cadatadir)" "$(DESTDIR)$(cadepdatadir)" \
	"$(DESTDIR)$(canecdatadir)" "$(DESTDIR)$(commoncorrecdatadir)" \
	"$(DESTDIR)$(commondatadir)" "$(DESTDIR)$(commonnecdatadir)" \
	"$(DESTDIR)$(configdatadir)" "$(DESTDIR)$(cychunkdir)" \
	"$(DESTDIR)$(cydatadir)" "$(DESTDIR)$(cydepdatadir)" \
	"$(DESTDIR)$(cynecdatadir)" "$(DESTDIR)$(enchunkdir)" \
	"$(DESTDIR)$(endatadir)" "$(DESTDIR)$(endepdatadir)" \
	"$(DESTDIR)$(ennecdatadir)" "$(DESTDIR)$(escorefdatadir)" \
	"$(DESTDIR)$(escorrecdatadir)" "$(DESTDIR)$(esdatadir)" \
	"$(DESTDIR)$(esdepdatadir)" "$(DESTDIR)$(esnecdatadir)" \
	"$(DESTDIR)$(esnerdatadir)" "$(DESTDIR)$(gldatadir)" \
	"$(DESTDIR)$(gldepdatadir)" "$(DESTDIR)$(glnecdatadir)" \
	"$(DESTDIR)$(itdatadir)" "$(DESTDIR)$(itnecdatadir)" \
	"$(DESTDIR)$(pkgdatadir)" "$(DESTDIR)$(ptdatadir)" \
	"$(DESTDIR)$(ptnecdatadir)"
DATA = $(asdata_DATA) $(asdepdata_DATA) $(asnecdata_DATA) \
	$(cacorefdata_DATA) $(cadata_DATA) $(cadepdata_DATA) \
	$(canecdata_DATA) $(commoncorrecdata_DATA) $(commondata_DATA) \
	$(commonnecdata_DATA) $(configdata_DATA) $(cychunk_DATA) \
	$(cydata_DATA) $(cydepdata_DATA) $(cynecdata_DATA) \
	$(enchunk_DATA) $(endata_DATA) $(endepdata_DATA) \
	$(ennecdata_DATA) $(escorefdata_DATA) $(escorrecdata_DATA) \
	$(esdata_DATA) $(esdepdata_DATA) $(esnecdata_DATA) \
	$(esnerdata_DATA) $(gldata_DATA) $(gldepdata_DATA) \
	$(glnecdata_DATA) $(itdata_DATA) $(itnecdata_DATA) \
	$(pkgdata_DATA) $(ptdata_DATA) $(ptnecdata_DATA)
DISTFILES = $(DIST_COMMON) $(DIST_SOURCES) $(TEXINFOS) $(EXTRA_DIST)
ACLOCAL = @ACLOCAL@
AMTAR = @AMTAR@
AR = @AR@
AUTOCONF = @AUTOCONF@
AUTOHEADER = @AUTOHEADER@
AUTOMAKE = @AUTOMAKE@
AWK = @AWK@
CC = @CC@
CCDEPMODE = @CCDEPMODE@
CFLAGS = @CFLAGS@
CPP = @CPP@
CPPFLAGS = @CPPFLAGS@
CXX = @CXX@
CXXCPP = @CXXCPP@
CXXDEPMODE = @CXXDEPMODE@
CXXFLAGS = @CXXFLAGS@
CYGPATH_W = @CYGPATH_W@
DEFS = @DEFS@
DEPDIR = @DEPDIR@
DSYMUTIL = @DSYMUTIL@
DUMPBIN = @DUMPBIN@
ECHO_C = @ECHO_C@
ECHO_N = @ECHO_N@
ECHO_T = @ECHO_T@
EGREP = @EGREP@
EXEEXT = @EXEEXT@
FGREP = @FGREP@
GREP = @GREP@
INSTALL = @INSTALL@
INSTALL_DATA = @INSTALL_DATA@
INSTALL_PROGRAM = @INSTALL_PROGRAM@
INSTALL_SCRIPT = @INSTALL_SCRIPT@
INSTALL_STRIP_PROGRAM = @INSTALL_STRIP_PROGRAM@
LD = @LD@
LDFLAGS = @LDFLAGS@
LIBOBJS = @LIBOBJS@
LIBS = @LIBS@
LIBTOOL = @LIBTOOL@
LIPO = @LIPO@
LN_S = @LN_S@
LTLIBOBJS = @LTLIBOBJS@
MAKEINFO = @MAKEINFO@
MKDIR_P = @MKDIR_P@
NM = @NM@
NMEDIT = @NMEDIT@
OBJDUMP = @OBJDUMP@
OBJEXT = @OBJEXT@
OTOOL = @OTOOL@
OTOOL64 = @OTOOL64@
PACKAGE = @PACKAGE@
PACKAGE_BUGREPORT = @PACKAGE_BUGREPORT@
PACKAGE_NAME = @PACKAGE_NAME@
PACKAGE_STRING = @PACKAGE_STRING@
PACKAGE_TARNAME = @PACKAGE_TARNAME@
PACKAGE_URL = @PACKAGE_URL@
PACKAGE_VERSION = @PACKAGE_VERSION@
PATH_SEPARATOR = @PATH_SEPARATOR@
RANLIB = @RANLIB@
SED = @SED@
SET_MAKE = @SET_MAKE@
SHELL = @SHELL@
STRIP = @STRIP@
VERSION = @VERSION@
abs_builddir = @abs_builddir@
abs_srcdir = @abs_srcdir@
abs_top_builddir = @abs_top_builddir@
abs_top_srcdir = @abs_top_srcdir@
ac_ct_CC = @ac_ct_CC@
ac_ct_CXX = @ac_ct_CXX@
ac_ct_DUMPBIN = @ac_ct_DUMPBIN@
am__include = @am__include@
am__leading_dot = @am__leading_dot@
am__quote = @am__quote@
am__tar = @am__tar@
am__untar = @am__untar@
bindir = @bindir@
build = @build@
build_alias = @build_alias@
build_cpu = @build_cpu@
build_os = @build_os@
build_vendor = @build_vendor@
builddir = @builddir@
datadir = @datadir@
datarootdir = @datarootdir@
docdir = @docdir@
dvidir = @dvidir@
exec_prefix = @exec_prefix@
host = @host@
host_alias = @host_alias@
host_cpu = @host_cpu@
host_os = @host_os@
host_vendor = @host_vendor@
htmldir = @htmldir@
includedir = @includedir@
infodir = @infodir@
install_sh = @install_sh@
libdir = @libdir@
libexecdir = @libexecdir@
localedir = @localedir@
localstatedir = @localstatedir@
lt_ECHO = @lt_ECHO@
mandir = @mandir@
mkdir_p = @mkdir_p@
oldincludedir = @oldincludedir@
pdfdir = @pdfdir@
prefix = @prefix@
program_transform_name = @program_transform_name@
psdir = @psdir@
sbindir = @sbindir@
sharedstatedir = @sharedstatedir@
srcdir = @srcdir@
sysconfdir = @sysconfdir@
target_alias = @target_alias@
top_build_prefix = @top_build_prefix@
top_builddir = @top_builddir@
top_srcdir = @top_srcdir@
languages = as ca cy en es gl it pt
ZCAT = gunzip -c
pkgdata_DATA = 
esdatadir = $(pkgdatadir)/es
esdata_DATA = es/*.*
esnecdatadir = $(pkgdatadir)/es/nec
esnecdata_DATA = es/nec/*
esnerdatadir = $(pkgdatadir)/es/ner
esnerdata_DATA = es/ner/*
esdepdatadir = $(pkgdatadir)/es/dep
esdepdata_DATA = es/dep/*
escorefdatadir = $(pkgdatadir)/es/coref
escorefdata_DATA = es/coref/*
escorrecdatadir = $(pkgdatadir)/es/corrector
escorrecdata_DATA = es/corrector/*
cadatadir = $(pkgdatadir)/ca
cadata_DATA = ca/*.*
canecdatadir = $(pkgdatadir)/ca/nec
canecdata_DATA = ca/nec/*
cadepdatadir = $(pkgdatadir)/ca/dep
cadepdata_DATA = ca/dep/*
cacorefdatadir = $(pkgdatadir)/ca/coref
cacorefdata_DATA = ca/coref/*
gldatadir = $(pkgdatadir)/gl
gldata_DATA = gl/*.*
glnecdatadir = $(pkgdatadir)/gl/nec
glnecdata_DATA = gl/nec/*
gldepdatadir = $(pkgdatadir)/gl/dep
gldepdata_DATA = gl/dep/*
asdatadir = $(pkgdatadir)/as
asdata_DATA = as/*.*
asnecdatadir = $(pkgdatadir)/as/nec
asnecdata_DATA = as/nec/*
asdepdatadir = $(pkgdatadir)/as/dep
asdepdata_DATA = as/dep/*
itdatadir = $(pkgdatadir)/it
itdata_DATA = it/*.*
itnecdatadir = $(pkgdatadir)/it/nec
itnecdata_DATA = it/nec/*
ptdatadir = $(pkgdatadir)/pt
ptdata_DATA = pt/*.*
ptnecdatadir = $(pkgdatadir)/pt/nec
ptnecdata_DATA = pt/nec/*
endatadir = $(pkgdatadir)/en
endata_DATA = en/*.*
ennecdatadir = $(pkgdatadir)/en/nec
ennecdata_DATA = en/nec/*
enchunkdir = $(pkgdatadir)/en/chunker
enchunk_DATA = en/chunker/*
endepdatadir = $(pkgdatadir)/en/dep
endepdata_DATA = en/dep/*
cydatadir = $(pkgdatadir)/cy
cydata_DATA = cy/*.*
cynecdatadir = $(pkgdatadir)/cy/nec
cynecdata_DATA = cy/nec/*
cychunkdir = $(pkgdatadir)/cy/chunker
cychunk_DATA = cy/chunker/*
cydepdatadir = $(pkgdatadir)/cy/dep
cydepdata_DATA = cy/dep/*
commondatadir = $(pkgdatadir)/common
commondata_DATA = common/*.*
commonnecdatadir = $(pkgdatadir)/common/nec
commonnecdata_DATA = common/nec/*
commoncorrecdatadir = $(pkgdatadir)/common/corrector
commoncorrecdata_DATA = common/corrector/*
configdatadir = $(pkgdatadir)/config
configdata_DATA = config/*
all: all-am

.SUFFIXES:
$(srcdir)/Makefile.in:  $(srcdir)/Makefile.am  $(am__configure_deps)
	@for dep in $?; do \
	  case '$(am__configure_deps)' in \
	    *$$dep*) \
	      ( cd $(top_builddir) && $(MAKE) $(AM_MAKEFLAGS) am--refresh ) \
	        && { if test -f $@; then exit 0; else break; fi; }; \
	      exit 1;; \
	  esac; \
	done; \
	echo ' cd $(top_srcdir) && $(AUTOMAKE) --gnu data/Makefile'; \
	$(am__cd) $(top_srcdir) && \
	  $(AUTOMAKE) --gnu data/Makefile
.PRECIOUS: Makefile
Makefile: $(srcdir)/Makefile.in $(top_builddir)/config.status
	@case '$?' in \
	  *config.status*) \
	    cd $(top_builddir) && $(MAKE) $(AM_MAKEFLAGS) am--refresh;; \
	  *) \
	    echo ' cd $(top_builddir) && $(SHELL) ./config.status $(subdir)/$@ $(am__depfiles_maybe)'; \
	    cd $(top_builddir) && $(SHELL) ./config.status $(subdir)/$@ $(am__depfiles_maybe);; \
	esac;

$(top_builddir)/config.status: $(top_srcdir)/configure $(CONFIG_STATUS_DEPENDENCIES)
	cd $(top_builddir) && $(MAKE) $(AM_MAKEFLAGS) am--refresh

$(top_srcdir)/configure:  $(am__configure_deps)
	cd $(top_builddir) && $(MAKE) $(AM_MAKEFLAGS) am--refresh
$(ACLOCAL_M4):  $(am__aclocal_m4_deps)
	cd $(top_builddir) && $(MAKE) $(AM_MAKEFLAGS) am--refresh
$(am__aclocal_m4_deps):

mostlyclean-libtool:
	-rm -f *.lo

clean-libtool:
	-rm -rf .libs _libs
install-asdataDATA: $(asdata_DATA)
	@$(NORMAL_INSTALL)
	test -z "$(asdatadir)" || $(MKDIR_P) "$(DESTDIR)$(asdatadir)"
	@list='$(asdata_DATA)'; test -n "$(asdatadir)" || list=; \
	for p in $$list; do \
	  if test -f "$$p"; then d=; else d="$(srcdir)/"; fi; \
	  echo "$$d$$p"; \
	done | $(am__base_list) | \
	while read files; do \
	  echo " $(INSTALL_DATA) $$files '$(DESTDIR)$(asdatadir)'"; \
	  $(INSTALL_DATA) $$files "$(DESTDIR)$(asdatadir)" || exit $$?; \
	done

uninstall-asdataDATA:
	@$(NORMAL_UNINSTALL)
	@list='$(asdata_DATA)'; test -n "$(asdatadir)" || list=; \
	files=`for p in $$list; do echo $$p; done | sed -e 's|^.*/||'`; \
	test -n "$$files" || exit 0; \
	echo " ( cd '$(DESTDIR)$(asdatadir)' && rm -f" $$files ")"; \
	cd "$(DESTDIR)$(asdatadir)" && rm -f $$files
install-asdepdataDATA: $(asdepdata_DATA)
	@$(NORMAL_INSTALL)
	test -z "$(asdepdatadir)" || $(MKDIR_P) "$(DESTDIR)$(asdepdatadir)"
	@list='$(asdepdata_DATA)'; test -n "$(asdepdatadir)" || list=; \
	for p in $$list; do \
	  if test -f "$$p"; then d=; else d="$(srcdir)/"; fi; \
	  echo "$$d$$p"; \
	done | $(am__base_list) | \
	while read files; do \
	  echo " $(INSTALL_DATA) $$files '$(DESTDIR)$(asdepdatadir)'"; \
	  $(INSTALL_DATA) $$files "$(DESTDIR)$(asdepdatadir)" || exit $$?; \
	done

uninstall-asdepdataDATA:
	@$(NORMAL_UNINSTALL)
	@list='$(asdepdata_DATA)'; test -n "$(asdepdatadir)" || list=; \
	files=`for p in $$list; do echo $$p; done | sed -e 's|^.*/||'`; \
	test -n "$$files" || exit 0; \
	echo " ( cd '$(DESTDIR)$(asdepdatadir)' && rm -f" $$files ")"; \
	cd "$(DESTDIR)$(asdepdatadir)" && rm -f $$files
install-asnecdataDATA: $(asnecdata_DATA)
	@$(NORMAL_INSTALL)
	test -z "$(asnecdatadir)" || $(MKDIR_P) "$(DESTDIR)$(asnecdatadir)"
	@list='$(asnecdata_DATA)'; test -n "$(asnecdatadir)" || list=; \
	for p in $$list; do \
	  if test -f "$$p"; then d=; else d="$(srcdir)/"; fi; \
	  echo "$$d$$p"; \
	done | $(am__base_list) | \
	while read files; do \
	  echo " $(INSTALL_DATA) $$files '$(DESTDIR)$(asnecdatadir)'"; \
	  $(INSTALL_DATA) $$files "$(DESTDIR)$(asnecdatadir)" || exit $$?; \
	done

uninstall-asnecdataDATA:
	@$(NORMAL_UNINSTALL)
	@list='$(asnecdata_DATA)'; test -n "$(asnecdatadir)" || list=; \
	files=`for p in $$list; do echo $$p; done | sed -e 's|^.*/||'`; \
	test -n "$$files" || exit 0; \
	echo " ( cd '$(DESTDIR)$(asnecdatadir)' && rm -f" $$files ")"; \
	cd "$(DESTDIR)$(asnecdatadir)" && rm -f $$files
install-cacorefdataDATA: $(cacorefdata_DATA)
	@$(NORMAL_INSTALL)
	test -z "$(cacorefdatadir)" || $(MKDIR_P) "$(DESTDIR)$(cacorefdatadir)"
	@list='$(cacorefdata_DATA)'; test -n "$(cacorefdatadir)" || list=; \
	for p in $$list; do \
	  if test -f "$$p"; then d=; else d="$(srcdir)/"; fi; \
	  echo "$$d$$p"; \
	done | $(am__base_list) | \
	while read files; do \
	  echo " $(INSTALL_DATA) $$files '$(DESTDIR)$(cacorefdatadir)'"; \
	  $(INSTALL_DATA) $$files "$(DESTDIR)$(cacorefdatadir)" || exit $$?; \
	done

uninstall-cacorefdataDATA:
	@$(NORMAL_UNINSTALL)
	@list='$(cacorefdata_DATA)'; test -n "$(cacorefdatadir)" || list=; \
	files=`for p in $$list; do echo $$p; done | sed -e 's|^.*/||'`; \
	test -n "$$files" || exit 0; \
	echo " ( cd '$(DESTDIR)$(cacorefdatadir)' && rm -f" $$files ")"; \
	cd "$(DESTDIR)$(cacorefdatadir)" && rm -f $$files
install-cadataDATA: $(cadata_DATA)
	@$(NORMAL_INSTALL)
	test -z "$(cadatadir)" || $(MKDIR_P) "$(DESTDIR)$(cadatadir)"
	@list='$(cadata_DATA)'; test -n "$(cadatadir)" || list=; \
	for p in $$list; do \
	  if test -f "$$p"; then d=; else d="$(srcdir)/"; fi; \
	  echo "$$d$$p"; \
	done | $(am__base_list) | \
	while read files; do \
	  echo " $(INSTALL_DATA) $$files '$(DESTDIR)$(cadatadir)'"; \
	  $(INSTALL_DATA) $$files "$(DESTDIR)$(cadatadir)" || exit $$?; \
	done

uninstall-cadataDATA:
	@$(NORMAL_UNINSTALL)
	@list='$(cadata_DATA)'; test -n "$(cadatadir)" || list=; \
	files=`for p in $$list; do echo $$p; done | sed -e 's|^.*/||'`; \
	test -n "$$files" || exit 0; \
	echo " ( cd '$(DESTDIR)$(cadatadir)' && rm -f" $$files ")"; \
	cd "$(DESTDIR)$(cadatadir)" && rm -f $$files
install-cadepdataDATA: $(cadepdata_DATA)
	@$(NORMAL_INSTALL)
	test -z "$(cadepdatadir)" || $(MKDIR_P) "$(DESTDIR)$(cadepdatadir)"
	@list='$(cadepdata_DATA)'; test -n "$(cadepdatadir)" || list=; \
	for p in $$list; do \
	  if test -f "$$p"; then d=; else d="$(srcdir)/"; fi; \
	  echo "$$d$$p"; \
	done | $(am__base_list) | \
	while read files; do \
	  echo " $(INSTALL_DATA) $$files '$(DESTDIR)$(cadepdatadir)'"; \
	  $(INSTALL_DATA) $$files "$(DESTDIR)$(cadepdatadir)" || exit $$?; \
	done

uninstall-cadepdataDATA:
	@$(NORMAL_UNINSTALL)
	@list='$(cadepdata_DATA)'; test -n "$(cadepdatadir)" || list=; \
	files=`for p in $$list; do echo $$p; done | sed -e 's|^.*/||'`; \
	test -n "$$files" || exit 0; \
	echo " ( cd '$(DESTDIR)$(cadepdatadir)' && rm -f" $$files ")"; \
	cd "$(DESTDIR)$(cadepdatadir)" && rm -f $$files
install-canecdataDATA: $(canecdata_DATA)
	@$(NORMAL_INSTALL)
	test -z "$(canecdatadir)" || $(MKDIR_P) "$(DESTDIR)$(canecdatadir)"
	@list='$(canecdata_DATA)'; test -n "$(canecdatadir)" || list=; \
	for p in $$list; do \
	  if test -f "$$p"; then d=; else d="$(srcdir)/"; fi; \
	  echo "$$d$$p"; \
	done | $(am__base_list) | \
	while read files; do \
	  echo " $(INSTALL_DATA) $$files '$(DESTDIR)$(canecdatadir)'"; \
	  $(INSTALL_DATA) $$files "$(DESTDIR)$(canecdatadir)" || exit $$?; \
	done

uninstall-canecdataDATA:
	@$(NORMAL_UNINSTALL)
	@list='$(canecdata_DATA)'; test -n "$(canecdatadir)" || list=; \
	files=`for p in $$list; do echo $$p; done | sed -e 's|^.*/||'`; \
	test -n "$$files" || exit 0; \
	echo " ( cd '$(DESTDIR)$(canecdatadir)' && rm -f" $$files ")"; \
	cd "$(DESTDIR)$(canecdatadir)" && rm -f $$files
install-commoncorrecdataDATA: $(commoncorrecdata_DATA)
	@$(NORMAL_INSTALL)
	test -z "$(commoncorrecdatadir)" || $(MKDIR_P) "$(DESTDIR)$(commoncorrecdatadir)"
	@list='$(commoncorrecdata_DATA)'; test -n "$(commoncorrecdatadir)" || list=; \
	for p in $$list; do \
	  if test -f "$$p"; then d=; else d="$(srcdir)/"; fi; \
	  echo "$$d$$p"; \
	done | $(am__base_list) | \
	while read files; do \
	  echo " $(INSTALL_DATA) $$files '$(DESTDIR)$(commoncorrecdatadir)'"; \
	  $(INSTALL_DATA) $$files "$(DESTDIR)$(commoncorrecdatadir)" || exit $$?; \
	done

uninstall-commoncorrecdataDATA:
	@$(NORMAL_UNINSTALL)
	@list='$(commoncorrecdata_DATA)'; test -n "$(commoncorrecdatadir)" || list=; \
	files=`for p in $$list; do echo $$p; done | sed -e 's|^.*/||'`; \
	test -n "$$files" || exit 0; \
	echo " ( cd '$(DESTDIR)$(commoncorrecdatadir)' && rm -f" $$files ")"; \
	cd "$(DESTDIR)$(commoncorrecdatadir)" && rm -f $$files
install-commondataDATA: $(commondata_DATA)
	@$(NORMAL_INSTALL)
	test -z "$(commondatadir)" || $(MKDIR_P) "$(DESTDIR)$(commondatadir)"
	@list='$(commondata_DATA)'; test -n "$(commondatadir)" || list=; \
	for p in $$list; do \
	  if test -f "$$p"; then d=; else d="$(srcdir)/"; fi; \
	  echo "$$d$$p"; \
	done | $(am__base_list) | \
	while read files; do \
	  echo " $(INSTALL_DATA) $$files '$(DESTDIR)$(commondatadir)'"; \
	  $(INSTALL_DATA) $$files "$(DESTDIR)$(commondatadir)" || exit $$?; \
	done

uninstall-commondataDATA:
	@$(NORMAL_UNINSTALL)
	@list='$(commondata_DATA)'; test -n "$(commondatadir)" || list=; \
	files=`for p in $$list; do echo $$p; done | sed -e 's|^.*/||'`; \
	test -n "$$files" || exit 0; \
	echo " ( cd '$(DESTDIR)$(commondatadir)' && rm -f" $$files ")"; \
	cd "$(DESTDIR)$(commondatadir)" && rm -f $$files
install-commonnecdataDATA: $(commonnecdata_DATA)
	@$(NORMAL_INSTALL)
	test -z "$(commonnecdatadir)" || $(MKDIR_P) "$(DESTDIR)$(commonnecdatadir)"
	@list='$(commonnecdata_DATA)'; test -n "$(commonnecdatadir)" || list=; \
	for p in $$list; do \
	  if test -f "$$p"; then d=; else d="$(srcdir)/"; fi; \
	  echo "$$d$$p"; \
	done | $(am__base_list) | \
	while read files; do \
	  echo " $(INSTALL_DATA) $$files '$(DESTDIR)$(commonnecdatadir)'"; \
	  $(INSTALL_DATA) $$files "$(DESTDIR)$(commonnecdatadir)" || exit $$?; \
	done

uninstall-commonnecdataDATA:
	@$(NORMAL_UNINSTALL)
	@list='$(commonnecdata_DATA)'; test -n "$(commonnecdatadir)" || list=; \
	files=`for p in $$list; do echo $$p; done | sed -e 's|^.*/||'`; \
	test -n "$$files" || exit 0; \
	echo " ( cd '$(DESTDIR)$(commonnecdatadir)' && rm -f" $$files ")"; \
	cd "$(DESTDIR)$(commonnecdatadir)" && rm -f $$files
install-configdataDATA: $(configdata_DATA)
	@$(NORMAL_INSTALL)
	test -z "$(configdatadir)" || $(MKDIR_P) "$(DESTDIR)$(configdatadir)"
	@list='$(configdata_DATA)'; test -n "$(configdatadir)" || list=; \
	for p in $$list; do \
	  if test -f "$$p"; then d=; else d="$(srcdir)/"; fi; \
	  echo "$$d$$p"; \
	done | $(am__base_list) | \
	while read files; do \
	  echo " $(INSTALL_DATA) $$files '$(DESTDIR)$(configdatadir)'"; \
	  $(INSTALL_DATA) $$files "$(DESTDIR)$(configdatadir)" || exit $$?; \
	done

uninstall-configdataDATA:
	@$(NORMAL_UNINSTALL)
	@list='$(configdata_DATA)'; test -n "$(configdatadir)" || list=; \
	files=`for p in $$list; do echo $$p; done | sed -e 's|^.*/||'`; \
	test -n "$$files" || exit 0; \
	echo " ( cd '$(DESTDIR)$(configdatadir)' && rm -f" $$files ")"; \
	cd "$(DESTDIR)$(configdatadir)" && rm -f $$files
install-cychunkDATA: $(cychunk_DATA)
	@$(NORMAL_INSTALL)
	test -z "$(cychunkdir)" || $(MKDIR_P) "$(DESTDIR)$(cychunkdir)"
	@list='$(cychunk_DATA)'; test -n "$(cychunkdir)" || list=; \
	for p in $$list; do \
	  if test -f "$$p"; then d=; else d="$(srcdir)/"; fi; \
	  echo "$$d$$p"; \
	done | $(am__base_list) | \
	while read files; do \
	  echo " $(INSTALL_DATA) $$files '$(DESTDIR)$(cychunkdir)'"; \
	  $(INSTALL_DATA) $$files "$(DESTDIR)$(cychunkdir)" || exit $$?; \
	done

uninstall-cychunkDATA:
	@$(NORMAL_UNINSTALL)
	@list='$(cychunk_DATA)'; test -n "$(cychunkdir)" || list=; \
	files=`for p in $$list; do echo $$p; done | sed -e 's|^.*/||'`; \
	test -n "$$files" || exit 0; \
	echo " ( cd '$(DESTDIR)$(cychunkdir)' && rm -f" $$files ")"; \
	cd "$(DESTDIR)$(cychunkdir)" && rm -f $$files
install-cydataDATA: $(cydata_DATA)
	@$(NORMAL_INSTALL)
	test -z "$(cydatadir)" || $(MKDIR_P) "$(DESTDIR)$(cydatadir)"
	@list='$(cydata_DATA)'; test -n "$(cydatadir)" || list=; \
	for p in $$list; do \
	  if test -f "$$p"; then d=; else d="$(srcdir)/"; fi; \
	  echo "$$d$$p"; \
	done | $(am__base_list) | \
	while read files; do \
	  echo " $(INSTALL_DATA) $$files '$(DESTDIR)$(cydatadir)'"; \
	  $(INSTALL_DATA) $$files "$(DESTDIR)$(cydatadir)" || exit $$?; \
	done

uninstall-cydataDATA:
	@$(NORMAL_UNINSTALL)
	@list='$(cydata_DATA)'; test -n "$(cydatadir)" || list=; \
	files=`for p in $$list; do echo $$p; done | sed -e 's|^.*/||'`; \
	test -n "$$files" || exit 0; \
	echo " ( cd '$(DESTDIR)$(cydatadir)' && rm -f" $$files ")"; \
	cd "$(DESTDIR)$(cydatadir)" && rm -f $$files
install-cydepdataDATA: $(cydepdata_DATA)
	@$(NORMAL_INSTALL)
	test -z "$(cydepdatadir)" || $(MKDIR_P) "$(DESTDIR)$(cydepdatadir)"
	@list='$(cydepdata_DATA)'; test -n "$(cydepdatadir)" || list=; \
	for p in $$list; do \
	  if test -f "$$p"; then d=; else d="$(srcdir)/"; fi; \
	  echo "$$d$$p"; \
	done | $(am__base_list) | \
	while read files; do \
	  echo " $(INSTALL_DATA) $$files '$(DESTDIR)$(cydepdatadir)'"; \
	  $(INSTALL_DATA) $$files "$(DESTDIR)$(cydepdatadir)" || exit $$?; \
	done

uninstall-cydepdataDATA:
	@$(NORMAL_UNINSTALL)
	@list='$(cydepdata_DATA)'; test -n "$(cydepdatadir)" || list=; \
	files=`for p in $$list; do echo $$p; done | sed -e 's|^.*/||'`; \
	test -n "$$files" || exit 0; \
	echo " ( cd '$(DESTDIR)$(cydepdatadir)' && rm -f" $$files ")"; \
	cd "$(DESTDIR)$(cydepdatadir)" && rm -f $$files
install-cynecdataDATA: $(cynecdata_DATA)
	@$(NORMAL_INSTALL)
	test -z "$(cynecdatadir)" || $(MKDIR_P) "$(DESTDIR)$(cynecdatadir)"
	@list='$(cynecdata_DATA)'; test -n "$(cynecdatadir)" || list=; \
	for p in $$list; do \
	  if test -f "$$p"; then d=; else d="$(srcdir)/"; fi; \
	  echo "$$d$$p"; \
	done | $(am__base_list) | \
	while read files; do \
	  echo " $(INSTALL_DATA) $$files '$(DESTDIR)$(cynecdatadir)'"; \
	  $(INSTALL_DATA) $$files "$(DESTDIR)$(cynecdatadir)" || exit $$?; \
	done

uninstall-cynecdataDATA:
	@$(NORMAL_UNINSTALL)
	@list='$(cynecdata_DATA)'; test -n "$(cynecdatadir)" || list=; \
	files=`for p in $$list; do echo $$p; done | sed -e 's|^.*/||'`; \
	test -n "$$files" || exit 0; \
	echo " ( cd '$(DESTDIR)$(cynecdatadir)' && rm -f" $$files ")"; \
	cd "$(DESTDIR)$(cynecdatadir)" && rm -f $$files
install-enchunkDATA: $(enchunk_DATA)
	@$(NORMAL_INSTALL)
	test -z "$(enchunkdir)" || $(MKDIR_P) "$(DESTDIR)$(enchunkdir)"
	@list='$(enchunk_DATA)'; test -n "$(enchunkdir)" || list=; \
	for p in $$list; do \
	  if test -f "$$p"; then d=; else d="$(srcdir)/"; fi; \
	  echo "$$d$$p"; \
	done | $(am__base_list) | \
	while read files; do \
	  echo " $(INSTALL_DATA) $$files '$(DESTDIR)$(enchunkdir)'"; \
	  $(INSTALL_DATA) $$files "$(DESTDIR)$(enchunkdir)" || exit $$?; \
	done

uninstall-enchunkDATA:
	@$(NORMAL_UNINSTALL)
	@list='$(enchunk_DATA)'; test -n "$(enchunkdir)" || list=; \
	files=`for p in $$list; do echo $$p; done | sed -e 's|^.*/||'`; \
	test -n "$$files" || exit 0; \
	echo " ( cd '$(DESTDIR)$(enchunkdir)' && rm -f" $$files ")"; \
	cd "$(DESTDIR)$(enchunkdir)" && rm -f $$files
install-endataDATA: $(endata_DATA)
	@$(NORMAL_INSTALL)
	test -z "$(endatadir)" || $(MKDIR_P) "$(DESTDIR)$(endatadir)"
	@list='$(endata_DATA)'; test -n "$(endatadir)" || list=; \
	for p in $$list; do \
	  if test -f "$$p"; then d=; else d="$(srcdir)/"; fi; \
	  echo "$$d$$p"; \
	done | $(am__base_list) | \
	while read files; do \
	  echo " $(INSTALL_DATA) $$files '$(DESTDIR)$(endatadir)'"; \
	  $(INSTALL_DATA) $$files "$(DESTDIR)$(endatadir)" || exit $$?; \
	done

uninstall-endataDATA:
	@$(NORMAL_UNINSTALL)
	@list='$(endata_DATA)'; test -n "$(endatadir)" || list=; \
	files=`for p in $$list; do echo $$p; done | sed -e 's|^.*/||'`; \
	test -n "$$files" || exit 0; \
	echo " ( cd '$(DESTDIR)$(endatadir)' && rm -f" $$files ")"; \
	cd "$(DESTDIR)$(endatadir)" && rm -f $$files
install-endepdataDATA: $(endepdata_DATA)
	@$(NORMAL_INSTALL)
	test -z "$(endepdatadir)" || $(MKDIR_P) "$(DESTDIR)$(endepdatadir)"
	@list='$(endepdata_DATA)'; test -n "$(endepdatadir)" || list=; \
	for p in $$list; do \
	  if test -f "$$p"; then d=; else d="$(srcdir)/"; fi; \
	  echo "$$d$$p"; \
	done | $(am__base_list) | \
	while read files; do \
	  echo " $(INSTALL_DATA) $$files '$(DESTDIR)$(endepdatadir)'"; \
	  $(INSTALL_DATA) $$files "$(DESTDIR)$(endepdatadir)" || exit $$?; \
	done

uninstall-endepdataDATA:
	@$(NORMAL_UNINSTALL)
	@list='$(endepdata_DATA)'; test -n "$(endepdatadir)" || list=; \
	files=`for p in $$list; do echo $$p; done | sed -e 's|^.*/||'`; \
	test -n "$$files" || exit 0; \
	echo " ( cd '$(DESTDIR)$(endepdatadir)' && rm -f" $$files ")"; \
	cd "$(DESTDIR)$(endepdatadir)" && rm -f $$files
install-ennecdataDATA: $(ennecdata_DATA)
	@$(NORMAL_INSTALL)
	test -z "$(ennecdatadir)" || $(MKDIR_P) "$(DESTDIR)$(ennecdatadir)"
	@list='$(ennecdata_DATA)'; test -n "$(ennecdatadir)" || list=; \
	for p in $$list; do \
	  if test -f "$$p"; then d=; else d="$(srcdir)/"; fi; \
	  echo "$$d$$p"; \
	done | $(am__base_list) | \
	while read files; do \
	  echo " $(INSTALL_DATA) $$files '$(DESTDIR)$(ennecdatadir)'"; \
	  $(INSTALL_DATA) $$files "$(DESTDIR)$(ennecdatadir)" || exit $$?; \
	done

uninstall-ennecdataDATA:
	@$(NORMAL_UNINSTALL)
	@list='$(ennecdata_DATA)'; test -n "$(ennecdatadir)" || list=; \
	files=`for p in $$list; do echo $$p; done | sed -e 's|^.*/||'`; \
	test -n "$$files" || exit 0; \
	echo " ( cd '$(DESTDIR)$(ennecdatadir)' && rm -f" $$files ")"; \
	cd "$(DESTDIR)$(ennecdatadir)" && rm -f $$files
install-escorefdataDATA: $(escorefdata_DATA)
	@$(NORMAL_INSTALL)
	test -z "$(escorefdatadir)" || $(MKDIR_P) "$(DESTDIR)$(escorefdatadir)"
	@list='$(escorefdata_DATA)'; test -n "$(escorefdatadir)" || list=; \
	for p in $$list; do \
	  if test -f "$$p"; then d=; else d="$(srcdir)/"; fi; \
	  echo "$$d$$p"; \
	done | $(am__base_list) | \
	while read files; do \
	  echo " $(INSTALL_DATA) $$files '$(DESTDIR)$(escorefdatadir)'"; \
	  $(INSTALL_DATA) $$files "$(DESTDIR)$(escorefdatadir)" || exit $$?; \
	done

uninstall-escorefdataDATA:
	@$(NORMAL_UNINSTALL)
	@list='$(escorefdata_DATA)'; test -n "$(escorefdatadir)" || list=; \
	files=`for p in $$list; do echo $$p; done | sed -e 's|^.*/||'`; \
	test -n "$$files" || exit 0; \
	echo " ( cd '$(DESTDIR)$(escorefdatadir)' && rm -f" $$files ")"; \
	cd "$(DESTDIR)$(escorefdatadir)" && rm -f $$files
install-escorrecdataDATA: $(escorrecdata_DATA)
	@$(NORMAL_INSTALL)
	test -z "$(escorrecdatadir)" || $(MKDIR_P) "$(DESTDIR)$(escorrecdatadir)"
	@list='$(escorrecdata_DATA)'; test -n "$(escorrecdatadir)" || list=; \
	for p in $$list; do \
	  if test -f "$$p"; then d=; else d="$(srcdir)/"; fi; \
	  echo "$$d$$p"; \
	done | $(am__base_list) | \
	while read files; do \
	  echo " $(INSTALL_DATA) $$files '$(DESTDIR)$(escorrecdatadir)'"; \
	  $(INSTALL_DATA) $$files "$(DESTDIR)$(escorrecdatadir)" || exit $$?; \
	done

uninstall-escorrecdataDATA:
	@$(NORMAL_UNINSTALL)
	@list='$(escorrecdata_DATA)'; test -n "$(escorrecdatadir)" || list=; \
	files=`for p in $$list; do echo $$p; done | sed -e 's|^.*/||'`; \
	test -n "$$files" || exit 0; \
	echo " ( cd '$(DESTDIR)$(escorrecdatadir)' && rm -f" $$files ")"; \
	cd "$(DESTDIR)$(escorrecdatadir)" && rm -f $$files
install-esdataDATA: $(esdata_DATA)
	@$(NORMAL_INSTALL)
	test -z "$(esdatadir)" || $(MKDIR_P) "$(DESTDIR)$(esdatadir)"
	@list='$(esdata_DATA)'; test -n "$(esdatadir)" || list=; \
	for p in $$list; do \
	  if test -f "$$p"; then d=; else d="$(srcdir)/"; fi; \
	  echo "$$d$$p"; \
	done | $(am__base_list) | \
	while read files; do \
	  echo " $(INSTALL_DATA) $$files '$(DESTDIR)$(esdatadir)'"; \
	  $(INSTALL_DATA) $$files "$(DESTDIR)$(esdatadir)" || exit $$?; \
	done

uninstall-esdataDATA:
	@$(NORMAL_UNINSTALL)
	@list='$(esdata_DATA)'; test -n "$(esdatadir)" || list=; \
	files=`for p in $$list; do echo $$p; done | sed -e 's|^.*/||'`; \
	test -n "$$files" || exit 0; \
	echo " ( cd '$(DESTDIR)$(esdatadir)' && rm -f" $$files ")"; \
	cd "$(DESTDIR)$(esdatadir)" && rm -f $$files
install-esdepdataDATA: $(esdepdata_DATA)
	@$(NORMAL_INSTALL)
	test -z "$(esdepdatadir)" || $(MKDIR_P) "$(DESTDIR)$(esdepdatadir)"
	@list='$(esdepdata_DATA)'; test -n "$(esdepdatadir)" || list=; \
	for p in $$list; do \
	  if test -f "$$p"; then d=; else d="$(srcdir)/"; fi; \
	  echo "$$d$$p"; \
	done | $(am__base_list) | \
	while read files; do \
	  echo " $(INSTALL_DATA) $$files '$(DESTDIR)$(esdepdatadir)'"; \
	  $(INSTALL_DATA) $$files "$(DESTDIR)$(esdepdatadir)" || exit $$?; \
	done

uninstall-esdepdataDATA:
	@$(NORMAL_UNINSTALL)
	@list='$(esdepdata_DATA)'; test -n "$(esdepdatadir)" || list=; \
	files=`for p in $$list; do echo $$p; done | sed -e 's|^.*/||'`; \
	test -n "$$files" || exit 0; \
	echo " ( cd '$(DESTDIR)$(esdepdatadir)' && rm -f" $$files ")"; \
	cd "$(DESTDIR)$(esdepdatadir)" && rm -f $$files
install-esnecdataDATA: $(esnecdata_DATA)
	@$(NORMAL_INSTALL)
	test -z "$(esnecdatadir)" || $(MKDIR_P) "$(DESTDIR)$(esnecdatadir)"
	@list='$(esnecdata_DATA)'; test -n "$(esnecdatadir)" || list=; \
	for p in $$list; do \
	  if test -f "$$p"; then d=; else d="$(srcdir)/"; fi; \
	  echo "$$d$$p"; \
	done | $(am__base_list) | \
	while read files; do \
	  echo " $(INSTALL_DATA) $$files '$(DESTDIR)$(esnecdatadir)'"; \
	  $(INSTALL_DATA) $$files "$(DESTDIR)$(esnecdatadir)" || exit $$?; \
	done

uninstall-esnecdataDATA:
	@$(NORMAL_UNINSTALL)
	@list='$(esnecdata_DATA)'; test -n "$(esnecdatadir)" || list=; \
	files=`for p in $$list; do echo $$p; done | sed -e 's|^.*/||'`; \
	test -n "$$files" || exit 0; \
	echo " ( cd '$(DESTDIR)$(esnecdatadir)' && rm -f" $$files ")"; \
	cd "$(DESTDIR)$(esnecdatadir)" && rm -f $$files
install-esnerdataDATA: $(esnerdata_DATA)
	@$(NORMAL_INSTALL)
	test -z "$(esnerdatadir)" || $(MKDIR_P) "$(DESTDIR)$(esnerdatadir)"
	@list='$(esnerdata_DATA)'; test -n "$(esnerdatadir)" || list=; \
	for p in $$list; do \
	  if test -f "$$p"; then d=; else d="$(srcdir)/"; fi; \
	  echo "$$d$$p"; \
	done | $(am__base_list) | \
	while read files; do \
	  echo " $(INSTALL_DATA) $$files '$(DESTDIR)$(esnerdatadir)'"; \
	  $(INSTALL_DATA) $$files "$(DESTDIR)$(esnerdatadir)" || exit $$?; \
	done

uninstall-esnerdataDATA:
	@$(NORMAL_UNINSTALL)
	@list='$(esnerdata_DATA)'; test -n "$(esnerdatadir)" || list=; \
	files=`for p in $$list; do echo $$p; done | sed -e 's|^.*/||'`; \
	test -n "$$files" || exit 0; \
	echo " ( cd '$(DESTDIR)$(esnerdatadir)' && rm -f" $$files ")"; \
	cd "$(DESTDIR)$(esnerdatadir)" && rm -f $$files
install-gldataDATA: $(gldata_DATA)
	@$(NORMAL_INSTALL)
	test -z "$(gldatadir)" || $(MKDIR_P) "$(DESTDIR)$(gldatadir)"
	@list='$(gldata_DATA)'; test -n "$(gldatadir)" || list=; \
	for p in $$list; do \
	  if test -f "$$p"; then d=; else d="$(srcdir)/"; fi; \
	  echo "$$d$$p"; \
	done | $(am__base_list) | \
	while read files; do \
	  echo " $(INSTALL_DATA) $$files '$(DESTDIR)$(gldatadir)'"; \
	  $(INSTALL_DATA) $$files "$(DESTDIR)$(gldatadir)" || exit $$?; \
	done

uninstall-gldataDATA:
	@$(NORMAL_UNINSTALL)
	@list='$(gldata_DATA)'; test -n "$(gldatadir)" || list=; \
	files=`for p in $$list; do echo $$p; done | sed -e 's|^.*/||'`; \
	test -n "$$files" || exit 0; \
	echo " ( cd '$(DESTDIR)$(gldatadir)' && rm -f" $$files ")"; \
	cd "$(DESTDIR)$(gldatadir)" && rm -f $$files
install-gldepdataDATA: $(gldepdata_DATA)
	@$(NORMAL_INSTALL)
	test -z "$(gldepdatadir)" || $(MKDIR_P) "$(DESTDIR)$(gldepdatadir)"
	@list='$(gldepdata_DATA)'; test -n "$(gldepdatadir)" || list=; \
	for p in $$list; do \
	  if test -f "$$p"; then d=; else d="$(srcdir)/"; fi; \
	  echo "$$d$$p"; \
	done | $(am__base_list) | \
	while read files; do \
	  echo " $(INSTALL_DATA) $$files '$(DESTDIR)$(gldepdatadir)'"; \
	  $(INSTALL_DATA) $$files "$(DESTDIR)$(gldepdatadir)" || exit $$?; \
	done

uninstall-gldepdataDATA:
	@$(NORMAL_UNINSTALL)
	@list='$(gldepdata_DATA)'; test -n "$(gldepdatadir)" || list=; \
	files=`for p in $$list; do echo $$p; done | sed -e 's|^.*/||'`; \
	test -n "$$files" || exit 0; \
	echo " ( cd '$(DESTDIR)$(gldepdatadir)' && rm -f" $$files ")"; \
	cd "$(DESTDIR)$(gldepdatadir)" && rm -f $$files
install-glnecdataDATA: $(glnecdata_DATA)
	@$(NORMAL_INSTALL)
	test -z "$(glnecdatadir)" || $(MKDIR_P) "$(DESTDIR)$(glnecdatadir)"
	@list='$(glnecdata_DATA)'; test -n "$(glnecdatadir)" || list=; \
	for p in $$list; do \
	  if test -f "$$p"; then d=; else d="$(srcdir)/"; fi; \
	  echo "$$d$$p"; \
	done | $(am__base_list) | \
	while read files; do \
	  echo " $(INSTALL_DATA) $$files '$(DESTDIR)$(glnecdatadir)'"; \
	  $(INSTALL_DATA) $$files "$(DESTDIR)$(glnecdatadir)" || exit $$?; \
	done

uninstall-glnecdataDATA:
	@$(NORMAL_UNINSTALL)
	@list='$(glnecdata_DATA)'; test -n "$(glnecdatadir)" || list=; \
	files=`for p in $$list; do echo $$p; done | sed -e 's|^.*/||'`; \
	test -n "$$files" || exit 0; \
	echo " ( cd '$(DESTDIR)$(glnecdatadir)' && rm -f" $$files ")"; \
	cd "$(DESTDIR)$(glnecdatadir)" && rm -f $$files
install-itdataDATA: $(itdata_DATA)
	@$(NORMAL_INSTALL)
	test -z "$(itdatadir)" || $(MKDIR_P) "$(DESTDIR)$(itdatadir)"
	@list='$(itdata_DATA)'; test -n "$(itdatadir)" || list=; \
	for p in $$list; do \
	  if test -f "$$p"; then d=; else d="$(srcdir)/"; fi; \
	  echo "$$d$$p"; \
	done | $(am__base_list) | \
	while read files; do \
	  echo " $(INSTALL_DATA) $$files '$(DESTDIR)$(itdatadir)'"; \
	  $(INSTALL_DATA) $$files "$(DESTDIR)$(itdatadir)" || exit $$?; \
	done

uninstall-itdataDATA:
	@$(NORMAL_UNINSTALL)
	@list='$(itdata_DATA)'; test -n "$(itdatadir)" || list=; \
	files=`for p in $$list; do echo $$p; done | sed -e 's|^.*/||'`; \
	test -n "$$files" || exit 0; \
	echo " ( cd '$(DESTDIR)$(itdatadir)' && rm -f" $$files ")"; \
	cd "$(DESTDIR)$(itdatadir)" && rm -f $$files
install-itnecdataDATA: $(itnecdata_DATA)
	@$(NORMAL_INSTALL)
	test -z "$(itnecdatadir)" || $(MKDIR_P) "$(DESTDIR)$(itnecdatadir)"
	@list='$(itnecdata_DATA)'; test -n "$(itnecdatadir)" || list=; \
	for p in $$list; do \
	  if test -f "$$p"; then d=; else d="$(srcdir)/"; fi; \
	  echo "$$d$$p"; \
	done | $(am__base_list) | \
	while read files; do \
	  echo " $(INSTALL_DATA) $$files '$(DESTDIR)$(itnecdatadir)'"; \
	  $(INSTALL_DATA) $$files "$(DESTDIR)$(itnecdatadir)" || exit $$?; \
	done

uninstall-itnecdataDATA:
	@$(NORMAL_UNINSTALL)
	@list='$(itnecdata_DATA)'; test -n "$(itnecdatadir)" || list=; \
	files=`for p in $$list; do echo $$p; done | sed -e 's|^.*/||'`; \
	test -n "$$files" || exit 0; \
	echo " ( cd '$(DESTDIR)$(itnecdatadir)' && rm -f" $$files ")"; \
	cd "$(DESTDIR)$(itnecdatadir)" && rm -f $$files
install-pkgdataDATA: $(pkgdata_DATA)
	@$(NORMAL_INSTALL)
	test -z "$(pkgdatadir)" || $(MKDIR_P) "$(DESTDIR)$(pkgdatadir)"
	@list='$(pkgdata_DATA)'; test -n "$(pkgdatadir)" || list=; \
	for p in $$list; do \
	  if test -f "$$p"; then d=; else d="$(srcdir)/"; fi; \
	  echo "$$d$$p"; \
	done | $(am__base_list) | \
	while read files; do \
	  echo " $(INSTALL_DATA) $$files '$(DESTDIR)$(pkgdatadir)'"; \
	  $(INSTALL_DATA) $$files "$(DESTDIR)$(pkgdatadir)" || exit $$?; \
	done

uninstall-pkgdataDATA:
	@$(NORMAL_UNINSTALL)
	@list='$(pkgdata_DATA)'; test -n "$(pkgdatadir)" || list=; \
	files=`for p in $$list; do echo $$p; done | sed -e 's|^.*/||'`; \
	test -n "$$files" || exit 0; \
	echo " ( cd '$(DESTDIR)$(pkgdatadir)' && rm -f" $$files ")"; \
	cd "$(DESTDIR)$(pkgdatadir)" && rm -f $$files
install-ptdataDATA: $(ptdata_DATA)
	@$(NORMAL_INSTALL)
	test -z "$(ptdatadir)" || $(MKDIR_P) "$(DESTDIR)$(ptdatadir)"
	@list='$(ptdata_DATA)'; test -n "$(ptdatadir)" || list=; \
	for p in $$list; do \
	  if test -f "$$p"; then d=; else d="$(srcdir)/"; fi; \
	  echo "$$d$$p"; \
	done | $(am__base_list) | \
	while read files; do \
	  echo " $(INSTALL_DATA) $$files '$(DESTDIR)$(ptdatadir)'"; \
	  $(INSTALL_DATA) $$files "$(DESTDIR)$(ptdatadir)" || exit $$?; \
	done

uninstall-ptdataDATA:
	@$(NORMAL_UNINSTALL)
	@list='$(ptdata_DATA)'; test -n "$(ptdatadir)" || list=; \
	files=`for p in $$list; do echo $$p; done | sed -e 's|^.*/||'`; \
	test -n "$$files" || exit 0; \
	echo " ( cd '$(DESTDIR)$(ptdatadir)' && rm -f" $$files ")"; \
	cd "$(DESTDIR)$(ptdatadir)" && rm -f $$files
install-ptnecdataDATA: $(ptnecdata_DATA)
	@$(NORMAL_INSTALL)
	test -z "$(ptnecdatadir)" || $(MKDIR_P) "$(DESTDIR)$(ptnecdatadir)"
	@list='$(ptnecdata_DATA)'; test -n "$(ptnecdatadir)" || list=; \
	for p in $$list; do \
	  if test -f "$$p"; then d=; else d="$(srcdir)/"; fi; \
	  echo "$$d$$p"; \
	done | $(am__base_list) | \
	while read files; do \
	  echo " $(INSTALL_DATA) $$files '$(DESTDIR)$(ptnecdatadir)'"; \
	  $(INSTALL_DATA) $$files "$(DESTDIR)$(ptnecdatadir)" || exit $$?; \
	done

uninstall-ptnecdataDATA:
	@$(NORMAL_UNINSTALL)
	@list='$(ptnecdata_DATA)'; test -n "$(ptnecdatadir)" || list=; \
	files=`for p in $$list; do echo $$p; done | sed -e 's|^.*/||'`; \
	test -n "$$files" || exit 0; \
	echo " ( cd '$(DESTDIR)$(ptnecdatadir)' && rm -f" $$files ")"; \
	cd "$(DESTDIR)$(ptnecdatadir)" && rm -f $$files
tags: TAGS
TAGS:

ctags: CTAGS
CTAGS:


distdir: $(DISTFILES)
	@srcdirstrip=`echo "$(srcdir)" | sed 's/[].[^$$\\*]/\\\\&/g'`; \
	topsrcdirstrip=`echo "$(top_srcdir)" | sed 's/[].[^$$\\*]/\\\\&/g'`; \
	list='$(DISTFILES)'; \
	  dist_files=`for file in $$list; do echo $$file; done | \
	  sed -e "s|^$$srcdirstrip/||;t" \
	      -e "s|^$$topsrcdirstrip/|$(top_builddir)/|;t"`; \
	case $$dist_files in \
	  */*) $(MKDIR_P) `echo "$$dist_files" | \
			   sed '/\//!d;s|^|$(distdir)/|;s,/[^/]*$$,,' | \
			   sort -u` ;; \
	esac; \
	for file in $$dist_files; do \
	  if test -f $$file || test -d $$file; then d=.; else d=$(srcdir); fi; \
	  if test -d $$d/$$file; then \
	    dir=`echo "/$$file" | sed -e 's,/[^/]*$$,,'`; \
	    if test -d "$(distdir)/$$file"; then \
	      find "$(distdir)/$$file" -type d ! -perm -700 -exec chmod u+rwx {} \;; \
	    fi; \
	    if test -d $(srcdir)/$$file && test $$d != $(srcdir); then \
	      cp -fpR $(srcdir)/$$file "$(distdir)$$dir" || exit 1; \
	      find "$(distdir)/$$file" -type d ! -perm -700 -exec chmod u+rwx {} \;; \
	    fi; \
	    cp -fpR $$d/$$file "$(distdir)$$dir" || exit 1; \
	  else \
	    test -f "$(distdir)/$$file" \
	    || cp -p $$d/$$file "$(distdir)/$$file" \
	    || exit 1; \
	  fi; \
	done
check-am: all-am
check: check-am
all-am: Makefile $(DATA)
installdirs:
	for dir in "$(DESTDIR)$(asdatadir)" "$(DESTDIR)$(asdepdatadir)" "$(DESTDIR)$(asnecdatadir)" "$(DESTDIR)$(cacorefdatadir)" "$(DESTDIR)$(cadatadir)" "$(DESTDIR)$(cadepdatadir)" "$(DESTDIR)$(canecdatadir)" "$(DESTDIR)$(commoncorrecdatadir)" "$(DESTDIR)$(commondatadir)" "$(DESTDIR)$(commonnecdatadir)" "$(DESTDIR)$(configdatadir)" "$(DESTDIR)$(cychunkdir)" "$(DESTDIR)$(cydatadir)" "$(DESTDIR)$(cydepdatadir)" "$(DESTDIR)$(cynecdatadir)" "$(DESTDIR)$(enchunkdir)" "$(DESTDIR)$(endatadir)" "$(DESTDIR)$(endepdatadir)" "$(DESTDIR)$(ennecdatadir)" "$(DESTDIR)$(escorefdatadir)" "$(DESTDIR)$(escorrecdatadir)" "$(DESTDIR)$(esdatadir)" "$(DESTDIR)$(esdepdatadir)" "$(DESTDIR)$(esnecdatadir)" "$(DESTDIR)$(esnerdatadir)" "$(DESTDIR)$(gldatadir)" "$(DESTDIR)$(gldepdatadir)" "$(DESTDIR)$(glnecdatadir)" "$(DESTDIR)$(itdatadir)" "$(DESTDIR)$(itnecdatadir)" "$(DESTDIR)$(pkgdatadir)" "$(DESTDIR)$(ptdatadir)" "$(DESTDIR)$(ptnecdatadir)"; do \
	  test -z "$$dir" || $(MKDIR_P) "$$dir"; \
	done
install: install-am
install-exec: install-exec-am
install-data: install-data-am
uninstall: uninstall-am

install-am: all-am
	@$(MAKE) $(AM_MAKEFLAGS) install-exec-am install-data-am

installcheck: installcheck-am
install-strip:
	$(MAKE) $(AM_MAKEFLAGS) INSTALL_PROGRAM="$(INSTALL_STRIP_PROGRAM)" \
	  install_sh_PROGRAM="$(INSTALL_STRIP_PROGRAM)" INSTALL_STRIP_FLAG=-s \
	  `test -z '$(STRIP)' || \
	    echo "INSTALL_PROGRAM_ENV=STRIPPROG='$(STRIP)'"` install
mostlyclean-generic:

clean-generic:

distclean-generic:
	-test -z "$(CONFIG_CLEAN_FILES)" || rm -f $(CONFIG_CLEAN_FILES)
	-test . = "$(srcdir)" || test -z "$(CONFIG_CLEAN_VPATH_FILES)" || rm -f $(CONFIG_CLEAN_VPATH_FILES)

maintainer-clean-generic:
	@echo "This command is intended for maintainers to use"
	@echo "it deletes files that may require special tools to rebuild."
clean: clean-am

clean-am: clean-generic clean-libtool mostlyclean-am

distclean: distclean-am
	-rm -f Makefile
distclean-am: clean-am distclean-generic

dvi: dvi-am

dvi-am:

html: html-am

html-am:

info: info-am

info-am:

install-data-am: install-asdataDATA install-asdepdataDATA \
	install-asnecdataDATA install-cacorefdataDATA \
	install-cadataDATA install-cadepdataDATA install-canecdataDATA \
	install-commoncorrecdataDATA install-commondataDATA \
	install-commonnecdataDATA install-configdataDATA \
	install-cychunkDATA install-cydataDATA install-cydepdataDATA \
	install-cynecdataDATA install-enchunkDATA install-endataDATA \
	install-endepdataDATA install-ennecdataDATA \
	install-escorefdataDATA install-escorrecdataDATA \
	install-esdataDATA install-esdepdataDATA install-esnecdataDATA \
	install-esnerdataDATA install-gldataDATA install-gldepdataDATA \
	install-glnecdataDATA install-itdataDATA install-itnecdataDATA \
	install-pkgdataDATA install-ptdataDATA install-ptnecdataDATA
	@$(NORMAL_INSTALL)
	$(MAKE) $(AM_MAKEFLAGS) install-data-hook
install-dvi: install-dvi-am

install-dvi-am:

install-exec-am:

install-html: install-html-am

install-html-am:

install-info: install-info-am

install-info-am:

install-man:

install-pdf: install-pdf-am

install-pdf-am:

install-ps: install-ps-am

install-ps-am:

installcheck-am:

maintainer-clean: maintainer-clean-am
	-rm -f Makefile
maintainer-clean-am: distclean-am maintainer-clean-generic

mostlyclean: mostlyclean-am

mostlyclean-am: mostlyclean-generic mostlyclean-libtool

pdf: pdf-am

pdf-am:

ps: ps-am

ps-am:

uninstall-am: uninstall-asdataDATA uninstall-asdepdataDATA \
	uninstall-asnecdataDATA uninstall-cacorefdataDATA \
	uninstall-cadataDATA uninstall-cadepdataDATA \
	uninstall-canecdataDATA uninstall-commoncorrecdataDATA \
	uninstall-commondataDATA uninstall-commonnecdataDATA \
	uninstall-configdataDATA uninstall-cychunkDATA \
	uninstall-cydataDATA uninstall-cydepdataDATA \
	uninstall-cynecdataDATA uninstall-enchunkDATA \
	uninstall-endataDATA uninstall-endepdataDATA \
	uninstall-ennecdataDATA uninstall-escorefdataDATA \
	uninstall-escorrecdataDATA uninstall-esdataDATA \
	uninstall-esdepdataDATA uninstall-esnecdataDATA \
	uninstall-esnerdataDATA uninstall-gldataDATA \
	uninstall-gldepdataDATA uninstall-glnecdataDATA \
	uninstall-itdataDATA uninstall-itnecdataDATA \
	uninstall-pkgdataDATA uninstall-ptdataDATA \
	uninstall-ptnecdataDATA
	@$(NORMAL_INSTALL)
	$(MAKE) $(AM_MAKEFLAGS) uninstall-hook
.MAKE: install-am install-data-am install-strip uninstall-am

.PHONY: all all-am check check-am clean clean-generic clean-libtool \
	distclean distclean-generic distclean-libtool distdir dvi \
	dvi-am html html-am info info-am install install-am \
	install-asdataDATA install-asdepdataDATA install-asnecdataDATA \
	install-cacorefdataDATA install-cadataDATA \
	install-cadepdataDATA install-canecdataDATA \
	install-commoncorrecdataDATA install-commondataDATA \
	install-commonnecdataDATA install-configdataDATA \
	install-cychunkDATA install-cydataDATA install-cydepdataDATA \
	install-cynecdataDATA install-data install-data-am \
	install-data-hook install-dvi install-dvi-am \
	install-enchunkDATA install-endataDATA install-endepdataDATA \
	install-ennecdataDATA install-escorefdataDATA \
	install-escorrecdataDATA install-esdataDATA \
	install-esdepdataDATA install-esnecdataDATA \
	install-esnerdataDATA install-exec install-exec-am \
	install-gldataDATA install-gldepdataDATA install-glnecdataDATA \
	install-html install-html-am install-info install-info-am \
	install-itdataDATA install-itnecdataDATA install-man \
	install-pdf install-pdf-am install-pkgdataDATA install-ps \
	install-ps-am install-ptdataDATA install-ptnecdataDATA \
	install-strip installcheck installcheck-am installdirs \
	maintainer-clean maintainer-clean-generic mostlyclean \
	mostlyclean-generic mostlyclean-libtool pdf pdf-am ps ps-am \
	uninstall uninstall-am uninstall-asdataDATA \
	uninstall-asdepdataDATA uninstall-asnecdataDATA \
	uninstall-cacorefdataDATA uninstall-cadataDATA \
	uninstall-cadepdataDATA uninstall-canecdataDATA \
	uninstall-commoncorrecdataDATA uninstall-commondataDATA \
	uninstall-commonnecdataDATA uninstall-configdataDATA \
	uninstall-cychunkDATA uninstall-cydataDATA \
	uninstall-cydepdataDATA uninstall-cynecdataDATA \
	uninstall-enchunkDATA uninstall-endataDATA \
	uninstall-endepdataDATA uninstall-ennecdataDATA \
	uninstall-escorefdataDATA uninstall-escorrecdataDATA \
	uninstall-esdataDATA uninstall-esdepdataDATA \
	uninstall-esnecdataDATA uninstall-esnerdataDATA \
	uninstall-gldataDATA uninstall-gldepdataDATA \
	uninstall-glnecdataDATA uninstall-hook uninstall-itdataDATA \
	uninstall-itnecdataDATA uninstall-pkgdataDATA \
	uninstall-ptdataDATA uninstall-ptnecdataDATA


install-data-hook:
	@echo "Indexing language dictionaries. This will take some minutes."
	@for lg in $(languages); do \
	   if [ -f "$(pkgdatadir)/$$lg/corrector/corrector.db" ]; \
	   	then rm -f $(pkgdatadir)/$$lg/corrector/corrector.db; \
	   fi; \
	   if [ -f "$(pkgdatadir)/$$lg/corrector/corrector.rules" ]; \
		then echo "...Creating sound database for "$$lg" dictionary, please wait."; \
		     $(ZCAT) -f $(pkgdatadir)/$$lg/dicc.src | $(bindir)/dicc2phon $(pkgdatadir)/$$lg/corrector/corrector.rules $(pkgdatadir)/common/corrector/corrector.soundDicFile $(pkgdatadir)/common/corrector/corrector.sampa  |$(bindir)/indexdict $(pkgdatadir)/$$lg/corrector/corrector.db; \
           fi; \
	   echo "...Indexing "$$lg" dictionary, please wait."; \
	   rm -f $(pkgdatadir)/$$lg/maco.db; \
	   $(ZCAT) -f $(pkgdatadir)/$$lg/dicc.src | $(bindir)/indexdict $(pkgdatadir)/$$lg/maco.db; \
	   echo "...Indexing "$$lg" sense dictionary, please wait."; \
	   for src in $(pkgdatadir)/$$lg/senses*.src; do \
             name=`echo $$src | sed 's/.src//' `; \
             rm -f $$name.db; \
	     $(ZCAT) -f $$src | $(bindir)/indexdict $$name.db; \
	     $(ZCAT) -f $$src | $(bindir)/convertdict > $$name.ukb; \
	   done \
	done 
	@echo "Indexing WN-based semantic database"
	@rm -f $(pkgdatadir)/common/wn30.db;
	@$(ZCAT) -f $(pkgdatadir)/common/wn30.src | $(bindir)/indexdict $(pkgdatadir)/common/wn30.db;
	@$(bindir)/compile_kb -o $(pkgdatadir)/common/wn30-ukb.bin $(pkgdatadir)/common/wnet30_rels.txt;
	@$(bindir)/compile_kb -o $(pkgdatadir)/common/xwn30-ukb.bin $(pkgdatadir)/common/wnet30_rels.txt $(pkgdatadir)/common/wnet30g_rels.txt

uninstall-hook:
	@rm -rf $(pkgdatadir)

# Tell versions [3.59,3.63) of GNU make to not export all variables.
# Otherwise a system limit (for SysV at least) may be exceeded.
.NOEXPORT:
